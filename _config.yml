title: SAHIL CHAWLA # name of the site
author: SAHIL CHAWLA # name of site's author
email: sahil.chawla@gmail.com # email of site's author
url: https://csahil.github.io # root address of the site
baseurl: "/portfolio" # subpath of the site, e.g. "/blog" (leave it blank "" if you're site shouldn't use a subpath)
description: > # description of the site (multiple lines allowed)
    I graduated UC Berkeley in 2020 with a double major in Economics and Interdisciplinary Studies (my own creation of psych/history/sociology).
    
    I'm interested in what cannot entirely be explained by either numbers or words, and have found that interest fulfilled by investing.

permalink: /:slug.html

favicon: "logo.png" # name+extension of favicon (which must be put on the root folder)
# goat_counter: "yoursitename" # put your GoatCounter name if you want to use GoatCounter analytics

remote_theme: no-style-please

theme_config:
  appearance: "light" # can be "light", "dark" or "auto"
  back_home_text: ".." # customize text for homepage link in post layout
  date_format: "%m-%Y" # customize how date is formatted
  show_description: true # show blog description in home page

sass:
  style: :compressed

plugins:
  - jekyll-feed
  - jekyll-seo-tag
